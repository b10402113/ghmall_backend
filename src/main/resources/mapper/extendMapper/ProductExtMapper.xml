<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.ghmall.dao.ProductMapper">
    <select id="selectByCategoryIdSet" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from ghmall_product
        where status = 1
        <if test="categoryIdSet.size() > 0">
            and category_id in
            <foreach collection="categoryIdSet" item="item" index="index" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
    </select>

    <select id="selectByCategoryIdSetAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from ghmall_product
        <if test="categoryIdSet.size() > 0">
            and category_id in
            <foreach collection="categoryIdSet" item="item" index="index" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="query != null and query != '' and query.length > 0">
            where name like CONCAT('%',#{query,jdbcType=VARCHAR},'%')
        </if>
    </select>

    <select id="selectByProductIdSet" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from ghmall_product
        <where>
            <if test="productIdSet.size() > 0">
                id in
                <foreach collection="productIdSet" item="item" index="index" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>

    <select id="selectByNameAndProductId" resultMap="BaseResultMap" parameterType="map">
        SELECT
        <include refid="Base_Column_List"/>
        from ghmall_product
        <where>
            <if test="productName != null">
                and name like #{productName}
            </if>
            <if test="productId != null">
                and id = #{productId}
            </if>
        </where>
    </select>
</mapper>